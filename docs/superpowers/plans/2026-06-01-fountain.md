# Fountain Placement and Animation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace broken random-scatter fountain props with correctly-placed gargoyle+basin pairs on inner room walls, add floor-adaptive basin rendering, and animate the gargoyle stream and basin ripple when the player activates the fountain with F.

**Architecture:** Six tasks in dependency order: (1) drain sprites, (2) DEPTH_THEMES cleanup, (3) paired placement in map.js, (4) buildEntities for new spawn kinds, (5) F-key toggle + fountainTime tick in game.js, (6) canvas animation rendering. Tasks 1–2 are order-independent; Tasks 3–6 must run sequentially.

**Tech Stack:** Vanilla JS, HTML5 Canvas, Electron, `node --test`

---

## File Map

| File | Change |
|---|---|
| `renderer/render/sprites.js` | Add `prop_drain_empty` (tile_0043) and `prop_drain_liquid` (tile_0044) |
| `renderer/data/levels.js` | Remove fountain/gargoyle/pipe entries from `DEPTH_THEMES` random room props |
| `renderer/systems/map.js` | Add paired fountain placement after regular prop scatter |
| `renderer/game.js` | Add `fountain_wall` / `fountain_basin` cases to `buildEntities`; add F-key toggle and `fountainTime` tick to `update()` |
| `renderer/render/canvas.js` | Update `drawEntity` prop block: basin clipping, basin ripple, gargoyle stream animation |
| `test/map.test.js` | Tests for paired fountain placement |

---

## Task 1: Add drain sprite entries

**Files:**
- Modify: `renderer/render/sprites.js`

- [ ] **Step 1: Add two drain sprite entries**

In `renderer/render/sprites.js`, find:

```js
  prop_gravestone:     'tile_0065',
  prop_grave:          'tile_0066',
```

Replace with:

```js
  prop_gravestone:     'tile_0065',
  prop_grave:          'tile_0066',
  prop_drain_empty:    'tile_0043',
  prop_drain_liquid:   'tile_0044',
```

- [ ] **Step 2: Run all tests**

```bash
cd /home/lappemikb/projects/dungeon-crawler && node --test test/
```

Expected: all 57 tests pass.

- [ ] **Step 3: Commit**

```bash
cd /home/lappemikb/projects/dungeon-crawler && git add renderer/render/sprites.js && git commit -m "feat: add drain sprite entries (tile_0043, tile_0044)"
```

---

## Task 2: Remove fountain props from random scatter

**Files:**
- Modify: `renderer/data/levels.js`

Gargoyle, pipe, and fountain props are now placed as explicit pairs by the map generator. Removing them from the random scatter list prevents duplicates.

- [ ] **Step 1: Update DEPTH_THEMES**

In `renderer/data/levels.js`, find and replace the `DEPTH_THEMES` export:

```js
export const DEPTH_THEMES = [
  {
    depths: [1, 2, 3],
    floorTile: 'floor',
    bgColor:  '#12121e',
    tint:     null,
    fogAlpha: 0.65,
    props: {
      room: ['prop_table', 'prop_chair', 'prop_anvil', 'prop_barrel'],
    },
  },
  {
    depths: [4, 5, 6],
    floorTile: 'sand',
    bgColor:  '#1a1206',
    tint:     'rgba(40,20,0,0.2)',
    fogAlpha: 0.65,
    props: {
      room: ['prop_gravestone', 'prop_anvil'],
    },
  },
  {
    depths: [7, 8, 9],
    floorTile: 'floor',
    bgColor:  '#07070f',
    tint:     'rgba(0,0,20,0.35)',
    fogAlpha: 0.80,
    props: {
      room: ['prop_gravestone', 'prop_grave'],
    },
  },
]
```

- [ ] **Step 2: Run all tests**

```bash
cd /home/lappemikb/projects/dungeon-crawler && node --test test/
```

Expected: all 57 tests pass.

- [ ] **Step 3: Commit**

```bash
cd /home/lappemikb/projects/dungeon-crawler && git add renderer/data/levels.js && git commit -m "refactor: remove fountain/gargoyle/pipe from random prop scatter (now placed as pairs)"
```

---

## Task 3: Paired fountain placement in map.js

**Files:**
- Modify: `renderer/systems/map.js`
- Modify: `test/map.test.js`

Pairs are only placed on stone-floor depths (1–3 and 7–9). Each room gets at most one pair. The wall-mounted sprite sits on an inner wall tile; the basin sits on the floor tile directly below.

- [ ] **Step 1: Write failing tests**

In `test/map.test.js`, add after the existing `describe('generateLevel', ...)` block:

```js
describe('paired fountain placement', () => {
  it('places fountain_wall directly above fountain_basin', () => {
    let pairsFound = 0
    for (let attempt = 0; attempt < 20; attempt++) {
      const { entitySpawns } = generateLevel(1)
      const walls  = entitySpawns.filter(s => s.kind === 'fountain_wall')
      const basins = entitySpawns.filter(s => s.kind === 'fountain_basin')
      assert.equal(walls.length, basins.length, 'wall and basin counts must match')
      for (const w of walls) {
        const b = basins.find(b => b.x === w.pairX && b.y === w.pairY)
        assert.ok(b, `fountain_wall at (${w.x},${w.y}) has no matching basin`)
        assert.equal(b.y, w.y + 1, 'basin must be exactly 1 tile below wall')
        assert.equal(b.pairX, w.x, 'basin.pairX must point back to wall x')
        assert.equal(b.pairY, w.y, 'basin.pairY must point back to wall y')
        pairsFound++
      }
    }
    // At 60% chance per room across 20 runs, we expect at least some pairs
    assert.ok(pairsFound > 0, 'expected at least one fountain pair across 20 generateLevel(1) calls')
  })

  it('does not place fountain pairs on sand-floor depths', () => {
    for (let attempt = 0; attempt < 10; attempt++) {
      const { entitySpawns } = generateLevel(4)
      const walls = entitySpawns.filter(s => s.kind === 'fountain_wall')
      assert.equal(walls.length, 0, `depth 4 must have no fountain_wall, found ${walls.length}`)
    }
  })
})
```

- [ ] **Step 2: Run to confirm tests fail**

```bash
cd /home/lappemikb/projects/dungeon-crawler && node --test test/map.test.js 2>&1 | tail -8
```

Expected: FAIL — `fountain_wall` kind not yet generated.

- [ ] **Step 3: Add paired fountain placement to generateLevel in map.js**

Read `renderer/systems/map.js`. Find the prop scattering block that ends with:

```js
    return { map, entitySpawns, playerSpawn, rooms }
```

Insert BEFORE that `return` (after the existing prop scatter loop):

```js
    // Place paired gargoyle+basin fountains on stone-floor levels only
    if (theme?.floorTile !== 'sand') {
      for (const room of rooms) {
        if (Math.random() > 0.6) continue  // 60% chance per room

        // Inner top wall: room.y is the top wall row; floor is at room.y + 1
        const candidates = []
        for (let x = room.x + 2; x < room.x + room.w - 2; x++) {
          const wy = room.y, fy = room.y + 1
          if (
            map[wy]?.[x]?.tile === TILE.WALL &&
            isWalkable(map[fy]?.[x]?.tile) &&
            !occupiedKeys.has(`${x},${wy}`) &&
            !occupiedKeys.has(`${x},${fy}`)
          ) candidates.push({ wx: x, wy, fx: x, fy })
        }

        if (candidates.length === 0) continue
        const pick = candidates[Math.floor(Math.random() * candidates.length)]

        entitySpawns.push({
          kind: 'fountain_wall', propType: 'prop_gargoyle_dry',
          x: pick.wx, y: pick.wy, pairX: pick.fx, pairY: pick.fy,
        })
        entitySpawns.push({
          kind: 'fountain_basin', propType: 'prop_fountain_empty',
          x: pick.fx, y: pick.fy, pairX: pick.wx, pairY: pick.wy,
        })
        occupiedKeys.add(`${pick.wx},${pick.wy}`)
        occupiedKeys.add(`${pick.fx},${pick.fy}`)
      }
    }
```

- [ ] **Step 4: Run tests**

```bash
cd /home/lappemikb/projects/dungeon-crawler && node --test test/
```

Expected: all tests pass including the two new fountain placement tests.

- [ ] **Step 5: Commit**

```bash
cd /home/lappemikb/projects/dungeon-crawler && git add renderer/systems/map.js test/map.test.js && git commit -m "feat: paired gargoyle+basin fountain placement on inner room walls"
```

---

## Task 4: buildEntities — handle fountain spawn kinds

**Files:**
- Modify: `renderer/game.js`

- [ ] **Step 1: Add fountain_wall and fountain_basin cases to buildEntities**

In `renderer/game.js`, find:

```js
      case 'prop':    return [{ type: 'prop', propType: s.propType, x: s.x, y: s.y }]
      default:        return []
```

Replace with:

```js
      case 'prop':           return [{ type: 'prop', propType: s.propType, x: s.x, y: s.y }]
      case 'fountain_wall':  return [{ type: 'prop', propType: s.propType, x: s.x, y: s.y,
        isFountainWall: true, flowing: false, fountainTime: 0, pairX: s.pairX, pairY: s.pairY }]
      case 'fountain_basin': return [{ type: 'prop', propType: s.propType, x: s.x, y: s.y,
        isFountainBasin: true, flowing: false, fountainTime: 0, pairX: s.pairX, pairY: s.pairY }]
      default:               return []
```

- [ ] **Step 2: Run all tests**

```bash
cd /home/lappemikb/projects/dungeon-crawler && node --test test/
```

Expected: all pass.

- [ ] **Step 3: Commit**

```bash
cd /home/lappemikb/projects/dungeon-crawler && git add renderer/game.js && git commit -m "feat: buildEntities handles fountain_wall and fountain_basin spawn kinds"
```

---

## Task 5: F-key toggle + fountainTime tick

**Files:**
- Modify: `renderer/game.js`

- [ ] **Step 1: Add F-key fountain toggle in update()**

In `renderer/game.js`, in the `update(delta)` function, find:

```js
  // Steal treasure
  if ((keys['x'] || keys['X']) && map[player.y]?.[player.x]?.tile === TILE.TREASURE) {
    state.gameOver = true; endRun(true); return
  }
```

Insert AFTER that block:

```js
  // Fountain toggle (F key — player must stand on basin tile)
  if (keys['f'] || keys['F']) {
    keys['f'] = false; keys['F'] = false
    const basin = state.entities.find(e =>
      e.type === 'prop' && e.isFountainBasin && e.x === player.x && e.y === player.y
    )
    if (basin) {
      basin.flowing = !basin.flowing
      basin.propType = basin.flowing ? 'prop_fountain_full' : 'prop_fountain_empty'
      if (!basin.flowing) basin.fountainTime = 0
      const wall = state.entities.find(e =>
        e.type === 'prop' && e.isFountainWall && e.x === basin.pairX && e.y === basin.pairY
      )
      if (wall) {
        wall.flowing = basin.flowing
        wall.propType = wall.flowing ? 'prop_gargoyle_flow' : 'prop_gargoyle_dry'
        if (!wall.flowing) wall.fountainTime = 0
      }
    }
  }
```

- [ ] **Step 2: Add fountainTime tick**

In `renderer/game.js`, in `update(delta)`, find:

```js
  // Player death
  if (player.hp <= 0) {
```

Insert BEFORE that line:

```js
  // Advance fountain animation timers
  for (const e of state.entities) {
    if (e.type === 'prop' && e.flowing) {
      e.fountainTime = (e.fountainTime ?? 0) + delta
    }
  }
```

- [ ] **Step 3: Run all tests**

```bash
cd /home/lappemikb/projects/dungeon-crawler && node --test test/
```

Expected: all pass.

- [ ] **Step 4: Commit**

```bash
cd /home/lappemikb/projects/dungeon-crawler && git add renderer/game.js && git commit -m "feat: F-key toggles fountain flow; fountainTime advances each frame"
```

---

## Task 6: Canvas rendering — basin clipping, ripple, gargoyle stream

**Files:**
- Modify: `renderer/render/canvas.js`

- [ ] **Step 1: Add animation helper constants and functions**

In `renderer/render/canvas.js`, after the `FIRE_PAL` and breath constants block, add:

```js
// ── Fountain animation ────────────────────────────────────────────────────────
// Pixel coords that differ between empty/full basin sprites (16×16 sprite space)
const BASIN_WATER_PX = [
  [7,0],[8,0],[7,1],[8,1],
  [4,2],[5,2],[6,2],[7,2],[8,2],[9,2],[10,2],[11,2],
  [4,3],[5,3],[6,3],[7,3],[8,3],[9,3],[10,3],[11,3],
  [4,4],[5,4],[6,4],[7,4],[8,4],[9,4],[10,4],[11,4],
  [4,5],[5,5],[6,5],[7,5],[8,5],[9,5],[10,5],[11,5],
  [4,6],[5,6],[6,6],[7,6],[8,6],[9,6],[10,6],[11,6],
]
// Pixel coords that differ between dry/flow gargoyle sprites (16×16 sprite space)
const GARG_STREAM_PX = [
  [7,10],[8,10],[7,11],[8,11],[7,12],[8,12],
  [7,13],[8,13],[7,14],[8,14],[7,15],[8,15],
]
// Water palette extracted from basin sprites
const W_DARK  = [37, 149, 106]
const W_MID   = [67, 225, 179]
const W_LIGHT = [105, 255, 212]

function waterLerp(a, b, t) {
  return [a[0]+(b[0]-a[0])*t, a[1]+(b[1]-a[1])*t, a[2]+(b[2]-a[2])*t]
}
function waterColor(s) {
  if (s < 0.33) return waterLerp(W_DARK,  W_MID,   s / 0.33)
  if (s < 0.66) return waterLerp(W_MID,   W_LIGHT, (s - 0.33) / 0.33)
  return               waterLerp(W_LIGHT, W_DARK,  (s - 0.66) / 0.34)
}

// Ripple expanding from top-center of basin (where stream enters water)
function drawBasinRipple(ctx, px, py, S, t) {
  const SC = S / 16
  for (const [spx, spy] of BASIN_WATER_PX) {
    const dx = spx + 0.5 - 7.5, dy = spy + 0.5 - 0.0
    const dist = Math.sqrt(dx * dx + dy * dy)
    const phase = t * 2.5 - dist * 1.8
    const s = (Math.sin(phase) + 1) / 2
    const amp = Math.max(0, 1 - dist / 9)
    const blend = s * amp + 0.5 * (1 - amp)
    const [r, g, b] = waterColor(blend)
    ctx.fillStyle = `rgb(${r},${g},${b})`
    ctx.fillRect(Math.round(px + spx * SC), Math.round(py + spy * SC), Math.ceil(SC), Math.ceil(SC))
  }
}

// Stream cycling downward from gargoyle mouth
function drawGargoyleStream(ctx, px, py, S, t) {
  const SC = S / 16
  for (const [spx, spy] of GARG_STREAM_PX) {
    const phase = t * 4 - spy * 0.7
    const s = (Math.sin(phase) + 1) / 2
    const [r, g, b] = waterColor(s)
    ctx.fillStyle = `rgb(${r},${g},${b})`
    ctx.fillRect(Math.round(px + spx * SC), Math.round(py + spy * SC), Math.ceil(SC), Math.ceil(SC))
  }
}
```

- [ ] **Step 2: Replace the prop block in drawEntity**

In `renderer/render/canvas.js`, find:

```js
  if (entity.type === 'prop') {
    const s = sprites[entity.propType]
    if (s) ctx.drawImage(s, px, py, S, S)
    return
  }
```

Replace with:

```js
  if (entity.type === 'prop') {
    const s = sprites[entity.propType]
    if (entity.isFountainBasin) {
      // Draw only top 11/16 rows of sprite — floor tile beneath shows through bottom
      if (s) ctx.drawImage(s, 0, 0, 16, 11, px, py, S, Math.round(S * 11 / 16))
      if (entity.flowing) drawBasinRipple(ctx, px, py, S, entity.fountainTime ?? 0)
      return
    }
    if (entity.isFountainWall) {
      if (s) ctx.drawImage(s, px, py, S, S)
      if (entity.flowing) drawGargoyleStream(ctx, px, py, S, entity.fountainTime ?? 0)
      return
    }
    if (s) ctx.drawImage(s, px, py, S, S)
    return
  }
```

- [ ] **Step 3: Run all tests**

```bash
cd /home/lappemikb/projects/dungeon-crawler && node --test test/
```

Expected: all pass (canvas.js has no unit tests; no regressions).

- [ ] **Step 4: Smoke test**

Run the game (`npm start`). On level 1–3:
- [ ] Gargoyle appears on inner wall of some rooms (dry sprite)
- [ ] Basin appears on floor tile directly below the gargoyle
- [ ] Bottom of basin blends with floor tile — no sand fringe on stone floors
- [ ] Walk onto basin tile, press F → gargoyle switches to flow sprite + stream animation starts cycling downward
- [ ] Basin ripple expands from top-center continuously
- [ ] Press F again → both revert to dry sprites, animation stops

```bash
cd /home/lappemikb/projects/dungeon-crawler && npm start
```

- [ ] **Step 5: Commit**

```bash
cd /home/lappemikb/projects/dungeon-crawler && git add renderer/render/canvas.js && git commit -m "feat: basin/gargoyle fountain animation — floor-adaptive clipping, ripple, stream"
```

---

## Final verification

```bash
cd /home/lappemikb/projects/dungeon-crawler && node --test test/
```

Expected: all tests pass.
